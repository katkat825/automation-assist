[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Run from the repo root so `from app...` and `from tests.conftest...` resolve.
pythonpath = .
addopts = -q --strict-markers
